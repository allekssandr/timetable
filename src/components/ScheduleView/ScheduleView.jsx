import React from "react";
import s from "./ScheduleView.module.css";

const ScheduleView = (props) => {
    return (
        <div className={s.scheduleView}>
            Расписание
        </div>
    )
};

export default ScheduleView;